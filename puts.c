#include<stdio.h>
#include<string.h>
void main(){
    char sname[20];
    char fname[20];
    printf("Enter student name : ");
    scanf("%s",&sname);
    printf("Enter father name : ");
    scanf("%s",&fname);
    puts(sname);
    puts(fname);
}
